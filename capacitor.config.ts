import { CapacitorConfig } from '@capacitor/cli'; // eslint-disable-line

const config: CapacitorConfig = {
  appId: 'uk.ac.ceh.irecord-st-helen',
  appName: 'iRecord St Helena App',
  bundledWebRuntime: false,
  webDir: 'build',
  cordova: {},
};

export default config;
